---
phase: 06-lazy-loading-api
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - src/hooks/usePoints.ts
  - src/components/dashboard/xp-dashboard.tsx
  - src/components/dashboard/xp-total.tsx
  - src/components/dashboard/month-nav.tsx
  - src/stores/scan.store.ts
autonomous: true
requirements: [LAZY-01, LAZY-02, LAZY-03, LAZY-04, LAZY-05]

must_haves:
  truths:
    - "Dashboard shows partial XP with '+' indicator while loading"
    - "Skeleton shown for months still loading"
    - "Navigation arrows work for both loaded and unloaded months"
    - "Current month refreshable via button, past months immutable"
    - "Fresh fetch on new site visit (no stale cached data)"
  artifacts:
    - path: "src/components/dashboard/xp-dashboard.tsx"
      provides: "Updated dashboard with lazy loading integration"
      exports: ["XPDashboard"]
    - path: "src/components/dashboard/xp-total.tsx"
      provides: "XP total with loading indicator"
      exports: ["XPTotal"]
    - path: "src/hooks/usePoints.ts"
      provides: "Updated points hook for per-month data"
      exports: ["usePoints", "useMonthPoints"]
  key_links:
    - from: "src/components/dashboard/xp-dashboard.tsx"
      to: "src/hooks/useMonthTransfers.ts"
      via: "useInitialMonthLoad"
      pattern: "useInitialMonthLoad\\("
    - from: "src/components/dashboard/xp-dashboard.tsx"
      to: "src/hooks/usePrefetchManager.ts"
      via: "usePrefetchManager hook"
      pattern: "usePrefetchManager\\("
    - from: "src/components/dashboard/xp-total.tsx"
      to: "partial XP display"
      via: "isPartial prop"
      pattern: "isPartial.*\\+"
---

<objective>
Integrate lazy loading into dashboard UI with partial data display.

Purpose: Update dashboard to show partial XP while loading, use skeletons for unloaded months, and trigger background prefetch. Per CONTEXT.md: "Display partial total XP with indicator (e.g., '12,450 XP+') while history loads."

Output: Updated dashboard components with lazy loading UX patterns.
</objective>

<execution_context>
@/Users/ttresse/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ttresse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lazy-loading-api-fetch-current-month-only-prefetch-3-previous-months-load-on-navigation-disable-refresh-for-past-months/06-CONTEXT.md
@.planning/phases/06-lazy-loading-api-fetch-current-month-only-prefetch-3-previous-months-load-on-navigation-disable-refresh-for-past-months/06-RESEARCH.md
@.planning/phases/06-lazy-loading-api-fetch-current-month-only-prefetch-3-previous-months-load-on-navigation-disable-refresh-for-past-months/06-01-SUMMARY.md
@.planning/phases/06-lazy-loading-api-fetch-current-month-only-prefetch-3-previous-months-load-on-navigation-disable-refresh-for-past-months/06-02-SUMMARY.md

@src/components/dashboard/xp-dashboard.tsx
@src/components/dashboard/xp-total.tsx
@src/components/dashboard/month-nav.tsx
@src/components/dashboard/dashboard-skeleton.tsx
@src/hooks/usePoints.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update usePoints for per-month calculation</name>
  <files>src/hooks/usePoints.ts</files>
  <action>
Refactor usePoints to work with lazy-loaded per-month data:

1. Create `useMonthPoints(wallet: string | null, monthKey: string)`:
   - Uses useMonthClassification to get classified data for single month
   - Calculates XP for that specific month only
   - Returns: `{ monthPoints, isLoading, isError }`

2. Create `useAggregatedPoints(wallet: string | null, loadedMonthKeys: Set<string>)`:
   - Aggregates XP across all loaded months
   - Uses useQueries to get data from all loaded months
   - Returns:
     - `totalXP`: Sum of XP from loaded months
     - `isPartial`: true if not all months are loaded yet
     - `monthsLoaded`: Number of months contributing to total
     - `monthsTotal`: Total months available

3. Keep existing `usePoints` for backwards compatibility during transition:
   - Mark as @deprecated
   - Will be cleaned up after dashboard integration verified

4. XP calculation per month:
   - Import calculateMonthPoints from points.ts (may need to create)
   - Or inline the calculation: sum of category XPs for that month
   - Use existing tier lookup logic from points.ts

Export all hooks. Mark old usePoints as deprecated.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Verify useMonthPoints returns XP for single month</manual>
  </verify>
  <done>useMonthPoints and useAggregatedPoints hooks exported</done>
</task>

<task type="auto">
  <name>Task 2: Update XPTotal for partial loading indicator</name>
  <files>src/components/dashboard/xp-total.tsx</files>
  <action>
Add support for partial data display:

1. Update props interface:
```typescript
interface XPTotalProps {
  totalXP: number;
  isPartial?: boolean; // New prop - show "+" when still loading
}
```

2. Update display logic:
   - If isPartial is true, append "+" after formatted XP
   - Example: "12,450 XP+" instead of "12,450 XP"
   - Use same formatting (formatXP or toLocaleString) for number

3. Visual styling for partial state:
   - Consider subtle animation or opacity to indicate loading
   - Per CONTEXT.md: "No explicit progress counter - skeletons imply loading"
   - Keep it minimal - just the "+" suffix is the primary indicator

4. Accessibility:
   - Add aria-label explaining partial state
   - Example: aria-label="12,450 XP and counting, more data loading"

Export component with updated props.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Verify XP displays with "+" when isPartial=true</manual>
  </verify>
  <done>XPTotal shows "+" indicator when isPartial prop is true</done>
</task>

<task type="auto">
  <name>Task 3: Integrate lazy loading into XPDashboard</name>
  <files>src/components/dashboard/xp-dashboard.tsx</files>
  <action>
Major refactor of XPDashboard to use new lazy loading hooks:

1. Replace old hooks with new:
   - Remove: usePoints, useClassifiedTransactions imports
   - Add: useInitialMonthLoad, usePrefetchManager, useMonthPoints, useMonthClassification

2. Initial load integration:
   - Call `useInitialMonthLoad(wallet)` to start 4-month parallel fetch
   - Use `isInitialLoadComplete` for skeleton vs content decision
   - Pass `loadedMonthKeys` to prefetch manager

3. Prefetch integration:
   - Call `usePrefetchManager(wallet, loadedMonthKeys)` after initial load
   - This triggers background prefetch of remaining months
   - No UI needed - runs silently in background

4. Navigation state:
   - Keep monthIndex state (0-11 for 12-month range)
   - Calculate monthKey from index using getLastNMonthKeys(12)[monthIndex]
   - Dynamic boundaries: disable prev/next based on available data, not fixed 0/11

5. Per-month data fetching:
   - Use `useMonthPoints(wallet, currentMonthKey)` for selected month
   - If month not loaded yet, show DashboardSkeleton for that month
   - Use `prioritizeMonth(monthKey)` when navigating to unloaded month

6. Partial total display:
   - Use `useAggregatedPoints` for total XP across all loaded months
   - Pass `isPartial` to XPTotal when not all months loaded

7. Refresh button:
   - Only show for current month (monthIndex === 11)
   - Call queryClient.invalidateQueries for current month key only
   - Past months should not be refreshable per CONTEXT.md

8. Update categoryData computation:
   - Use per-month classification instead of full classifiedData
   - Same logic, just different data source

Remove old monolithic data flow. Build passes without old hooks.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Verify dashboard loads 4 months initially, prefetches rest</manual>
  </verify>
  <done>Dashboard uses lazy loading with partial XP display and background prefetch</done>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run build` passes
2. Initial page load only fetches 4 months (check Network tab)
3. Background prefetch visible in Network tab after initial load
4. XP total shows "+" while loading more months
5. Navigation to unloaded month shows skeleton then content
6. Past months don't refetch on navigation
7. Refresh button only refreshes current month
</verification>

<success_criteria>
- Dashboard shows partial XP with "+" while history loads
- Skeleton displayed for months still loading
- Navigation works for both loaded and unloaded months
- Unloaded month navigation triggers immediate fetch
- Current month refreshable via button
- Past months immutable (no refresh, no refetch)
- Background prefetch runs silently after initial 4 months
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-lazy-loading-api-fetch-current-month-only-prefetch-3-previous-months-load-on-navigation-disable-refresh-for-past-months/06-03-SUMMARY.md`
</output>
