---
phase: 04-dashboard-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/format.ts
  - src/lib/next-tier.ts
  - src/components/ui/skeleton.tsx
  - src/components/ui/badge.tsx
autonomous: true
requirements: [SCAN-05]

must_haves:
  truths:
    - "formatXP(12450) returns '12,450 XP'"
    - "formatUSD(2500) returns '$2,500'"
    - "formatMonthLabel('2026-03') returns 'March 2026'"
    - "getNextTierInfo returns distance to next tier with unit"
    - "Skeleton component available for loading states"
    - "Badge component available for tier badges"
  artifacts:
    - path: "src/lib/format.ts"
      provides: "Number and date formatting utilities"
      exports: ["formatXP", "formatUSD", "formatMonthLabel"]
    - path: "src/lib/next-tier.ts"
      provides: "Next tier calculation utility"
      exports: ["getNextTierInfo"]
    - path: "src/components/ui/skeleton.tsx"
      provides: "shadcn Skeleton component"
    - path: "src/components/ui/badge.tsx"
      provides: "shadcn Badge component"
  key_links:
    - from: "src/lib/next-tier.ts"
      to: "src/lib/tiers-config.ts"
      via: "getCategoryConfig import"
      pattern: "getCategoryConfig\\("
---

<objective>
Create formatting utilities and install shadcn components needed for dashboard.

Purpose: Establish foundation (number formatting, tier distance calculation, UI components) before building dashboard views.
Output: Format utilities (formatXP, formatUSD, formatMonthLabel), next tier utility (getNextTierInfo), Skeleton and Badge components installed.
</objective>

<execution_context>
@/Users/ttresse/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ttresse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-visualization/04-CONTEXT.md
@.planning/phases/04-dashboard-visualization/04-RESEARCH.md
@src/lib/tiers-config.ts
@src/lib/tiers-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Skeleton and Badge components</name>
  <files>src/components/ui/skeleton.tsx, src/components/ui/badge.tsx</files>
  <action>
Run shadcn CLI to install Skeleton and Badge components:

```bash
npx shadcn@latest add skeleton badge
```

Verify both files are created in src/components/ui/.
  </action>
  <verify>
    <automated>ls src/components/ui/skeleton.tsx src/components/ui/badge.tsx</automated>
  </verify>
  <done>Skeleton and Badge components installed and available for import</done>
</task>

<task type="auto">
  <name>Task 2: Create formatting utilities</name>
  <files>src/lib/format.ts</files>
  <action>
Create formatting utility functions per RESEARCH.md patterns:

1. **formatXP(value: number): string** - Format as "12,450 XP" using Intl.NumberFormat('en-US')
2. **formatUSD(value: number): string** - Format as "$2,500" using Intl.NumberFormat with style: 'currency', minimumFractionDigits: 0, maximumFractionDigits: 0
3. **formatMonthLabel(monthKey: string): string** - Convert "YYYY-MM" to "March 2026" using Intl.DateTimeFormat with month: 'long', year: 'numeric', timeZone: 'UTC'
4. **formatCount(value: number, unit: string): string** - Format as "15 transactions" or "7 chains"

Use module-level formatter instances (new Intl.NumberFormat) for reuse, not created per call.

Per RESEARCH.md: Use UTC for date parsing to avoid timezone issues.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/format.ts</automated>
  </verify>
  <done>All four format functions exported, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create next tier calculation utility</name>
  <files>src/lib/next-tier.ts</files>
  <action>
Create utility to calculate distance to next tier:

```typescript
import { getCategoryConfig } from './tiers-config';
import type { CategoryId } from './tiers-types';

export interface NextTierInfo {
  tierName: string;
  distance: number;
  unit: string;
}

export function getNextTierInfo(
  categoryId: CategoryId,
  currentValue: number
): NextTierInfo | null
```

Implementation per RESEARCH.md Pattern 2:
1. Get category config using getCategoryConfig(categoryId)
2. If no config, return null
3. Reverse the tiers array (to ascending order since stored descending)
4. Find first tier where tier.threshold > currentValue
5. If no next tier (already at max), return null
6. Return { tierName: tier.name, distance: tier.threshold - currentValue, unit: config.unit }

Per CONTEXT.md: Show distance to next tier for each category row.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/next-tier.ts</automated>
  </verify>
  <done>getNextTierInfo function exported, returns NextTierInfo or null for max tier</done>
</task>

</tasks>

<verification>
1. `ls src/components/ui/skeleton.tsx src/components/ui/badge.tsx` - Both files exist
2. `npx tsc --noEmit` - No TypeScript errors in new files
3. Format utilities use Intl API (no external dependencies)
</verification>

<success_criteria>
- Skeleton and Badge components installed via shadcn CLI
- formatXP, formatUSD, formatMonthLabel, formatCount functions available
- getNextTierInfo calculates distance to next tier correctly
- All files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-visualization/04-01-SUMMARY.md`
</output>
