---
phase: 04-dashboard-visualization
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/dashboard/xp-total.tsx
  - src/components/dashboard/category-row.tsx
  - src/components/dashboard/month-nav.tsx
  - src/components/dashboard/dashboard-skeleton.tsx
  - src/components/dashboard/xp-dashboard.tsx
  - src/app/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-05, DASH-06]

must_haves:
  truths:
    - "User sees total XP for selected month at top of dashboard"
    - "User sees all four categories with XP, tier badge, and next tier distance"
    - "User can navigate between months using arrow buttons"
    - "Dashboard shows skeleton placeholders while loading"
    - "Empty state shows friendly message for wallets with no transactions"
  artifacts:
    - path: "src/components/dashboard/xp-total.tsx"
      provides: "Total XP hero section"
      min_lines: 15
    - path: "src/components/dashboard/category-row.tsx"
      provides: "Single category display row"
      min_lines: 25
    - path: "src/components/dashboard/month-nav.tsx"
      provides: "Month navigation arrows"
      min_lines: 20
    - path: "src/components/dashboard/dashboard-skeleton.tsx"
      provides: "Loading state skeleton"
      min_lines: 20
    - path: "src/components/dashboard/xp-dashboard.tsx"
      provides: "Main dashboard container"
      min_lines: 50
    - path: "src/app/page.tsx"
      provides: "Updated page with dashboard integration"
  key_links:
    - from: "src/components/dashboard/xp-dashboard.tsx"
      to: "src/hooks/usePoints.ts"
      via: "usePoints hook import"
      pattern: "usePoints\\("
    - from: "src/components/dashboard/category-row.tsx"
      to: "src/lib/format.ts"
      via: "formatXP/formatUSD imports"
      pattern: "formatXP|formatUSD"
    - from: "src/components/dashboard/month-nav.tsx"
      to: "src/lib/format.ts"
      via: "formatMonthLabel import"
      pattern: "formatMonthLabel"
---

<objective>
Build the XP dashboard UI with all components and integrate into main page.

Purpose: Display XP breakdown with monthly navigation per CONTEXT.md locked decisions.
Output: Complete dashboard showing total XP, category breakdown with tier info, month navigation, loading/empty states.
</objective>

<execution_context>
@/Users/ttresse/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ttresse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-visualization/04-CONTEXT.md
@.planning/phases/04-dashboard-visualization/04-RESEARCH.md
@.planning/phases/04-dashboard-visualization/04-01-SUMMARY.md
@src/hooks/usePoints.ts
@src/lib/points-types.ts
@src/lib/format.ts
@src/lib/next-tier.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard components</name>
  <files>src/components/dashboard/xp-total.tsx, src/components/dashboard/category-row.tsx, src/components/dashboard/month-nav.tsx, src/components/dashboard/dashboard-skeleton.tsx</files>
  <action>
Create directory src/components/dashboard/ and four components:

**1. xp-total.tsx** - Total XP hero section
Per CONTEXT.md: "big number + 'XP' label only (no extra decoration)"
- Props: totalXP: number
- Display: Large centered number with "XP" suffix
- Use formatXP from format.ts (without the "XP" suffix since we show it separately for styling)
- Tailwind: text-center, text-5xl or text-6xl for big number, "XP" as smaller suffix

**2. category-row.tsx** - Single category display row
Per CONTEXT.md: "category name, XP value, tier badge, distance to next tier, USD volume"
- Props: category: CategoryPoints, volume: number | undefined, nextTierInfo: NextTierInfo | null
- Use formatXP for XP display
- Use formatUSD for volume (only for bridgoor/swapoor)
- Use formatCount for transaction count (transactoor) and chain count (chainoor)
- Badge component for tier name (variant="secondary")
- Show distance: "{distance} {unit} to {tierName}" when nextTierInfo present
- Per CONTEXT.md: Fixed order, all four always shown

**3. month-nav.tsx** - Month navigation
Per CONTEXT.md: "Arrow buttons (< and >) to step through months"
- Props: monthKey: string, canGoPrev: boolean, canGoNext: boolean, onPrev: () => void, onNext: () => void
- Use ChevronLeft/ChevronRight from lucide-react
- Button variant="ghost" size="icon" with disabled state
- formatMonthLabel for month display between arrows
- min-w-[150px] for stable width during navigation

**4. dashboard-skeleton.tsx** - Loading skeleton
Per CONTEXT.md: "Skeleton placeholders while loading"
- Match dimensions of actual content to prevent layout shift (per RESEARCH.md Pitfall 3)
- Skeleton for total XP section
- Skeleton for 4 category rows
- Skeleton for month navigation
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/dashboard/*.tsx</automated>
  </verify>
  <done>Four dashboard components created, all TypeScript valid</done>
</task>

<task type="auto">
  <name>Task 2: Create main dashboard container and integrate</name>
  <files>src/components/dashboard/xp-dashboard.tsx, src/app/page.tsx</files>
  <action>
**1. xp-dashboard.tsx** - Main dashboard container
Create container component that:
- Takes wallet: string | null prop
- Uses usePoints(wallet) hook
- Manages monthIndex state (default to 11 = current month per RESEARCH.md Pitfall 1)
- Reset monthIndex to 11 when wallet changes (per RESEARCH.md Pitfall 4)
- Renders:
  - DashboardSkeleton when isLoading
  - Error state with retry button when error
  - Empty state message when isComplete && transfers.length === 0 ("No Jumper transactions found for this wallet")
  - Full dashboard when pointsData available:
    - MonthNav at top
    - XPTotal showing currentMonth.totalXP
    - Four CategoryRow components in fixed order: transactoor, bridgoor, swapoor, chainoor
    - Refresh button at bottom
- For each category row, compute:
  - volume: bridgoor/swapoor get USD from MonthlyAggregate, transactoor/chainoor show count with unit
  - nextTierInfo from getNextTierInfo(categoryId, rawValue)

**2. page.tsx** - Update main page
Replace current success state ("Found X transactions") with XPDashboard component.
- Import XPDashboard
- Pass walletAddress to XPDashboard
- Remove old transfers display logic
- Keep WalletInput and ScanProgress as-is
- Keep "Scan a different wallet" link

Per CONTEXT.md: "Stacked single column layout", "UI is minimal, single-page"
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -50</automated>
  </verify>
  <done>Dashboard container wired to usePoints, page.tsx shows dashboard instead of transaction count</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of dashboard</name>
  <what-built>
Complete XP dashboard with:
- Total XP display at top
- Four category rows with tier badges and next tier distance
- Month navigation arrows
- Loading skeleton
- Empty state handling
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000
2. Enter a wallet address with known LiFi transactions (e.g., the test wallet used during development)
3. Verify loading: Skeleton placeholders appear during scan
4. Verify dashboard layout after load:
   - Total XP shown at top as big number
   - Four categories visible (TRANSACTOOR, BRIDGOOR, SWAPOOR, CHAINOOR)
   - Each row shows: category name, XP with commas, tier badge, next tier info
   - BRIDGOOR and SWAPOOR show USD values
   - TRANSACTOOR shows transaction count, CHAINOOR shows chain count
5. Verify month navigation:
   - Click < arrow - previous month loads
   - Click > arrow - next month loads
   - Current month name displayed between arrows
6. Test empty wallet:
   - Enter wallet with no LiFi transactions
   - Verify "No Jumper transactions found" message appears
7. Verify "Scan a different wallet" link still works

Expected: Clean, minimal dashboard matching CONTEXT.md specifications
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` - Successful build with no errors
2. Visual check - Dashboard matches CONTEXT.md layout requirements
3. Month navigation - Arrows work, month label updates
4. Empty state - Friendly message for wallets with no transactions
</verification>

<success_criteria>
- Total XP displayed prominently at top
- All four categories shown with XP, tier badge, distance to next tier
- BRIDGOOR/SWAPOOR show USD volumes, TRANSACTOOR shows count, CHAINOOR shows chain count
- Month navigation works with < > arrows
- Skeleton loading state displayed during scan
- Empty state shows friendly message
- UI is minimal and comprehensible within 5 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-visualization/04-02-SUMMARY.md`
</output>
