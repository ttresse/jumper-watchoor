---
phase: 01-foundation-data-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/adapters/covalent.adapter.ts
  - src/hooks/useScanWallet.ts
  - src/lib/types.ts
  - package.json
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "No Covalent code remains in the codebase"
    - "Deprecated dependencies are removed from package.json"
    - "Build succeeds without Covalent references"
  artifacts:
    - path: "src/adapters/covalent.adapter.ts"
      provides: "DELETED - file should not exist"
      deleted: true
    - path: "src/hooks/useScanWallet.ts"
      provides: "DELETED - file should not exist"
      deleted: true
  key_links: []
---

<objective>
Remove deprecated Covalent implementation code and dependencies after LiFi migration is complete.

Purpose: Clean up the codebase by removing:
1. Old Covalent adapter that is no longer used
2. Old useScanWallet hook that is replaced by useLiFiTransfers
3. Covalent-specific types that are no longer needed
4. Unused npm dependencies (@covalenthq/client-sdk, p-throttle)

This prevents confusion, reduces bundle size, and ensures no accidental usage of deprecated code.

Output: Cleaner codebase with only LiFi implementation
</objective>

<execution_context>
@/Users/ttresse/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ttresse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-layer/01-VERIFICATION.md

<!-- Verify LiFi implementation is complete before cleanup -->
@src/adapters/lifi.adapter.ts
@src/hooks/useLiFiTransfers.ts
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated adapter and hook files</name>
  <files>src/adapters/covalent.adapter.ts, src/hooks/useScanWallet.ts</files>
  <action>
Delete the following deprecated files:

1. `src/adapters/covalent.adapter.ts` - Old Covalent API adapter, replaced by `lifi.adapter.ts`
2. `src/hooks/useScanWallet.ts` - Old hook using Covalent, replaced by `useLiFiTransfers.ts`

Before deleting, verify:
- `src/adapters/lifi.adapter.ts` exists and exports `fetchAllTransfers`
- `src/hooks/useLiFiTransfers.ts` exists and exports `useLiFiTransfers`
- `src/app/page.tsx` imports from `useLiFiTransfers`, NOT `useScanWallet`

If any verification fails, do NOT delete the files - report the issue instead.

Use `rm` command or file system operations to delete.
  </action>
  <verify>
    <automated>! test -f src/adapters/covalent.adapter.ts && ! test -f src/hooks/useScanWallet.ts && echo "Files deleted successfully"</automated>
  </verify>
  <done>covalent.adapter.ts and useScanWallet.ts no longer exist in the codebase</done>
</task>

<task type="auto">
  <name>Task 2: Clean up types.ts - remove Covalent-specific types</name>
  <files>src/lib/types.ts</files>
  <action>
Review `src/lib/types.ts` and remove any Covalent-specific types that are no longer used:

Types to potentially remove (if they only served Covalent):
- `ChainResult` - was used for chain-by-chain scanning results
- Any types that reference Covalent data structures

Types to KEEP:
- `SupportedChain` - may still be useful for chain metadata
- Any types that are used by the new LiFi implementation

After removal, ensure:
- No TypeScript errors in the project
- LiFi types remain in `lifi-types.ts` (not this file)

If `types.ts` becomes empty after cleanup, consider if it should be deleted or kept for future use.
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "TypeScript compiles successfully"</automated>
  </verify>
  <done>types.ts contains only actively used types, no Covalent-specific types remain</done>
</task>

<task type="auto">
  <name>Task 3: Remove unused npm dependencies</name>
  <files>package.json</files>
  <action>
Remove deprecated dependencies that were only used by the Covalent implementation:

```bash
npm uninstall @covalenthq/client-sdk p-throttle
```

These packages are no longer needed because:
- `@covalenthq/client-sdk`: LiFi API uses native fetch, no SDK needed
- `p-throttle`: LiFi is a single endpoint, no rate limiting required (was used for parallel chain queries)

After removal:
- Verify `package.json` no longer contains these dependencies
- Run `npm install` to update lock file
- Ensure `npm run build` still succeeds
  </action>
  <verify>
    <automated>! grep -q "covalenthq/client-sdk\|p-throttle" package.json && npm run build && echo "Dependencies removed and build succeeds"</automated>
  </verify>
  <done>package.json has no Covalent/p-throttle dependencies, build succeeds</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `git status` shows deleted files and modified package.json
2. `npm run build` succeeds
3. No grep hits for "covalent" in src/ directory (case-insensitive)
4. No grep hits for "useScanWallet" in src/ directory
</verification>

<success_criteria>
- Covalent adapter and useScanWallet hook files deleted
- No Covalent-specific types remain in types.ts
- @covalenthq/client-sdk and p-throttle removed from package.json
- Project builds successfully
- No references to deprecated code remain
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md`
</output>
